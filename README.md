# AgentCore CLI

A structural rewrite of the AgentCore CLI aimed at establishing consistent conventions: Result-based error handling,
modules with dependency injection and minimal public interfaces, and tests that exercise real code paths via injected
implementations rather than mocks.

This is a work in progress. It mostly serves as a scratch pad for ideas of abstractions that may make sense in
refactoring the CLI.
